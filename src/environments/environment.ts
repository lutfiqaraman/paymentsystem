// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyCvjkhoW46gHjrUg0fs0SB_gFK_uE9XSBc',
    authDomain: 'pscrud.firebaseapp.com',
    databaseURL: 'https://pscrud.firebaseio.com',
    projectId: 'pscrud',
    storageBucket: 'pscrud.appspot.com',
    messagingSenderId: '998585259994',
    appId: '1:998585259994:web:3d16560ddbc60a756c52e3',
    measurementId: 'G-DF042MMCQY'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
